import styled from 'styled-components'
import {CModal} from '@coreui/react'

export const CModalContainer = styled(CModal)`
  height: 700px;
`

export const CModalTextarea = styled.textarea`
  height: 250px;
  width: 100%;
  border: none;
  padding: 10px;
`
